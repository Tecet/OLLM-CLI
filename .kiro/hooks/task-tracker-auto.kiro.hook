{
  "enabled": true,
  "name": "Auto Task Tracker",
  "description": "Automatically monitors task markdown files and prompts to update timestamps and status when tasks are modified",
  "version": "1",
  "when": {
    "type": "fileEdited",
    "patterns": [
      "**/tasks.md",
      ".kiro/specs/**/tasks.md"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "A task file has been edited. Review the changes and if a task status has changed (e.g., from [ ] to [x] or vice versa), run the appropriate task tracker command:\n\n- If a task was just marked as started, run: npm run task:start <file-path> <task-number>\n- If a task was just marked as completed, run: npm run task:complete <file-path> <task-number>\n- After completing any task work, remind the user to log Kiro credits with: npm run task:credits <file-path> <task-number> <credits> (they can find the credits amount in the chat window after each agent response)\n- To check overall progress, run: npm run task:status <file-path>\n\nOnly run these commands if you detect actual task status changes in the diff."
  }
}